export EDITOR='mvim -f'

# cd
alias ..="cd .."
alias cdd="cd -"
alias -g ...='../..'
alias -g ....='../../..'
alias -g .....='../../../..'

# Finder
alias o="open . &"
alias ff="open -a Firefox"

# Gems
alias gi="sudo gem install"
alias giv="sudo gem install -v"

# Git
alias g="git status"
alias gst="git status"
alias gb="git branch"
alias gp="git push"
alias gl="git pull"
alias gadd="git add -u && git add . && git status"
alias gci="git pull --rebase && rake && git push"
alias gc="git commit"
alias gca="git commit -a"
alias gco="git checkout"
alias gcb="git checkout -b"
#alias gm="git merge"
alias gconf="$EDITOR .git/config"

function gg() {
  git commit -v -a -m "$*"
}

function gbt() {
  git branch --track $2 $1/$2
  git checkout $2
}

function grf() {
  rm $1
  git checkout $1
}

# ls
alias l="ls -laGh"

# Rails
alias migrate="rake db:migrate db:test:prepare"
alias remigrate="rake db:migrate && rake db:migrate:redo && rake db:schema:dump db:test:prepare"
alias cuc="cucumber -r features"
alias sg="./script/generate"
alias sp="./script/plugin install"
alias r="rake"
alias rr="touch tmp/restart.txt"
alias ss='script/server'
alias sc='script/console'
alias a='autospec -rails'

function migrate_test (){
  env RAILS_ENV=test rake db:migrate
}


# Alias
alias reload="source ~/.aliases"
alias ea="mate -w ~/.aliases && reload"

alias m="mate ."

# VIM
alias e="mvim"

#function bundle() {
#  env ARCHFLAGS="-arch x86_64" /usr/bin/bundle $*
#}

#########
#  SSH  #
#########
alias devt='ssh -L7777:localhost:3306 -N ubuntu@sandbox.thefutureis.mobi'
alias dev='ssh ubuntu@sandbox.thefutureis.mobi'

alias stage='ssh bnferguson@stage.thefutureis.mobi'
alias staget='ssh -L7777:localhost:3306 -N bnferguson@stage.thefutureis.mobi'
#alias stage='ssh ubuntu@stage.thefutureis.mobi'
alias demo='ssh ubuntu@demo.thefutureis.mobi'
alias demot='ssh -L7777:localhost:3306 -N ubuntu@demo.thefutureis.mobi'
alias prod='ssh ubuntu@portal.thefutureis.mobi'
alias prodt='ssh -L7777:localhost:3306 -N ubuntu@portal.thefutureis.mobi'

alias a1='ssh bnferguson@kenny.thefutureis.mobi'
alias a1t='ssh -L7777:localhost:3306 -N bnferguson@kenny.thefutureis.mobi'
alias d1='ssh bnferguson@garrison.thefutureis.mobi'
alias d1t='ssh -L7777:localhost:3306 -N bnferguson@garrison.thefutureis.mobi'

#export GEM_EDITOR='mate'

if [[ -s /Users/bnferguson/.rvm/scripts/rvm ]] ; then source /Users/bnferguson/.rvm/scripts/rvm ; fi
